No peeking! (15 pts)

Hidden inside this exe file is a flag

Up to you to find it

============================
Walkthrough:

Firstly, we'll try to detect if we are able to get the flag using `strings` tool:

```
$ strings -eS noPEEKing.exe | grep -i brixel
C:\temp\BrixelCTF2020\challenges\reverse engineering and cracking\no peeking\source\noPEEKing\noPEEKing\obj\Release\noPEEKing.pdb

$ strings -el noPEEKing.exe | grep -i brixel
The flag is brixelCTF{d0tP33K}

```

Got it.
The flag is:

brixelCTF{d0tP33K}